# factor-zoo 长时间运行：给 Claude 的简短交接

日期：2026-09-10。目的：共同诊断性能并决定下一步，不是宣布计算失败。

> 后续状态（2026-09-12）：下文保留为 9 月 10 日的历史观察，不是当前进程状态。
> 该运行已于 9 月 11 日正常结束；新展示报告已完成无重算重放。
> 两次墙钟时长、来源和剩余性能边界见 [验证记录](VALIDATION_20260912.md)
> 与 [耗时观察](RUNTIME_OBSERVATIONS_20260912.json)。不再需要决定是否停止这个已结束的运行。

## 1. 当前状态

- 分支 `codex/fza-universe-wiring`，运行源码为 `de1e836`；远端工作分支已核实同一提交，尚未合并 main。
- 新增三层 vintage 对照、标签/持有期一致性阻断、宽度提示、PlausibilityRule 和文档契约检查已提交。
- 两个环境各覆盖 271 个测试并通过：完整 demo 模块 14 项 + 其余 257 项，分批运行，零失败/错误/跳过。此前两个整套运行各有一条精确措辞断言失败，恢复原句后重验；不是一次性 271 全通过。
- 200 家完整报告于 **9 月 9 日 18:10（英国时间）**启动。截至 **9 月 10 日约 15:30** 的检查，已约 21 小时，进程仍在，新归档目录尚不存在。
- 已输出十个因子的基础结果，以及截至 bm_ratio 的 vintage 对照。没有最终退出码，不能宣布生成或验收完成。

## 2. 已定位的当前热点

对实际计算进程 PID `22912` 做了 20 秒、10 Hz 的 py-spy 非暂停采样；没有重启进程、注入代码或读取局部变量。工具独立安装于忽略目录，没有修改锁定研究环境。

实际调用链：

```text
demo.main
  → compare_vintages：重新调用 PIT compute_factor
    → earnings_to_price（ep_ratio）
      → _ttm_fundamental_panel
        → _ttm_value
          → pandas 分组、sort_values、itertuples
```

- 117 个成功样本中，101 个包含 `_ttm_value`，76 个落在其 `group.sort_values("period_end").itertuples()` 所在行。
- 同时有 80 次采样失败，并出现解释器符号地址警告；成功样本能定位当前路径，**不能据此推断整个 21 小时的耗时占比**。
- 一次约 60 秒的观察中累计 CPU 增加约 49.5 秒；另一次 I/O 采样读写接近零。说明进程有计算活动，**不证明业务进度健康，也不排除低效重复工作**。

源码可确认的工作形状：

1. 基础报告已经计算过 PIT；`compare_vintages` 又计算一次 PIT，再算 restated。
2. `_ttm_fundamental_panel` 按 `(ticker, signal_date)` 分组，每组调用 `_ttm_value`。
3. `_ttm_value` 对可见会计区间反复转换日期、排序、去重，按起始日期分组，再逐组排序和迭代。
4. demo 在整次因子比较返回后才打印结果，三个最终文件在全部计算后才写出；因此无新输出不能直接判定挂死。

## 3. 尚未证明什么

- 未取得逐证券/逐日期进度，也没有证明是在重复同一组或陷入无限循环。
- 未得到全程 profile；21 小时墙钟时间不能全部归因于这 20 秒采样中的热点。
- 未量化复用 PIT、缓存 TTM 或重写分组逻辑分别能节省多少时间。
- 未重跑真实结果，不能证明任何拟议优化保持原数值、样本键、缺失语义及 vintage 隔离。

## 4. 请 Claude 重点评审

1. **当前运行怎么办？** 继续保留现有运行，还是在证据充分后停止、优化并重跑？目前没有停止/重启授权，不要默认已同意丢弃这次运行。
2. **先优化哪里？** 比较“复用已有 PIT FactorRun”和“减少 TTM 分组/排序开销”的收益、风险和验证成本。采样只支持优先调查，不支持保证某种方案必然提速。
3. **复用的契约是什么？** 必须一致的输入包括源码、数据、日期、universe、配置、行业映射和读取语义；不能跨 PIT/restated 误共享。确认 read-path 日志、诊断计数及来源记录是否依赖重新执行。
4. **如何验证不改定义？** 用明确生成的会计区间 fixture 做差分验证，覆盖 FY/YTD/季度重叠、重述、缺失、排序与重复区间；再比较全日期网格的数值与计数。不能仅用旧报告汇总数字证明所有中间键一致。
5. **如何改善可观测性？** 下次运行加阶段计时、适度进度输出和可验证检查点，避免整段计算完成前没有可判断的进度；不要在当前进程中热改源码。

## 5. 边界与证据位置

- 不缩短 185 个信号日期的完整网格、不改公式/标签/阈值、不扩样本、不升级研究证据。新报告必须逐项对照已提交的 `SELECTED_200_RERUN_EXPECTATIONS.md`。
- 输入库 `data/fza_200.duckdb` SHA-256：`66c60785d1c085d4883f49796a33f20ad2bdf418072086fbfd938100666312d9`。
- 原会话 `34743`；等待写出的新目录：`examples/outputs/b_selected_200_rerun_20260909/`。旧归档不覆盖。
- 本地采样：`.diagnostic-envs/report-profile-20260910.txt`。此文件及诊断脚本被 Git 忽略，**不会随分支 push 自动交付**。
- 关键源码：`src/fza/demo.py`、`src/fza/pipeline/run.py`、`src/fza/factors/library.py`。以上定位以提交 `de1e836` 为准。
- Git 小问题已独立解释：push 成功，但 fetch refspec 只跟踪旧分支 `codex/m4.1-p0-data-semantics`，因此本地缺少新工作分支的 `origin/...` 引用。未改配置，这不是性能问题。

**希望得到的回复：先指出诊断证据还缺什么，再给出最小风险的优化顺序与验收条件；不要仅凭进程存活或测试通过宣称长运行正常。**
